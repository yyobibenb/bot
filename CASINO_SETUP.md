# 🎰 Casino Bot - Инструкция по запуску

## 📋 Что реализовано

### ✅ Готовые компоненты:

1. **База данных PostgreSQL**
   - 15+ таблиц (пользователи, балансы, транзакции, игры, рефералы, дуэли)
   - Автоматические миграции при старте
   - Модели для работы с БД

2. **Игра "Кубик"** 🎲
   - 8 режимов игры (Больше/Меньше, Четное/Нечетное, Грань, Дуэль и т.д.)
   - Полный backend с обработкой ставок
   - Frontend с анимацией и обратной связью
   - Обновление баланса в реальном времени

3. **Mini App интерфейс**
   - 3 экрана: Профиль, Игры, Приглашение
   - Стеклянный дизайн iOS 17
   - Плавная навигация
   - Реферальная система (frontend)

## 🚀 Быстрый старт

### 1. Установка PostgreSQL

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

**macOS:**
```bash
brew install postgresql
brew services start postgresql
```

**Windows:**
Скачайте установщик с https://www.postgresql.org/download/windows/

### 2. Создание базы данных

```bash
# Войти в PostgreSQL
sudo -u postgres psql

# Создать базу данных
CREATE DATABASE casino_bot;

# Создать пользователя (опционально)
CREATE USER casino_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE casino_bot TO casino_user;

# Выйти
\q
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Получите токен от @BotFather в Telegram
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Настройки PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=casino_bot

# Порт сервера
PORT=5000

# URL вашего приложения (для локальной разработки)
WEB_APP_URL=http://localhost:5000
```

### 4. Установка зависимостей

```bash
npm install
```

### 5. Запуск

```bash
# Режим разработки
npm run dev

# Или production
npm run build
npm start
```

## 🎮 Настройка Telegram бота

### 1. Создайте бота через @BotFather

```
/newbot
```

Следуйте инструкциям и получите токен.

### 2. Настройте Mini App

```
/newapp
```

Или если бот уже создан:
```
/myapps
```

- Выберите вашего бота
- Укажите Web App URL: `https://your-domain.com` (или ngrok для тестирования)
- Загрузите иконку и описание

### 3. Настройте кнопку меню

```
/setmenubutton
```

Выберите бота и укажите:
- Text: 🎰 Играть
- URL: ваш Web App URL

## 🧪 Тестирование локально

Для локального тестирования используйте ngrok:

```bash
# Установите ngrok
npm install -g ngrok

# Запустите ngrok
ngrok http 5000

# Используйте полученный HTTPS URL в настройках бота
```

Обновите `WEB_APP_URL` в `.env` на URL от ngrok.

## 📊 Структура базы данных

### Основные таблицы:

- **users** - пользователи
- **balances** - балансы
- **transactions** - транзакции (пополнение/вывод)
- **games** - игры
- **game_modes** - режимы игр
- **game_history** - история игр
- **referrals** - реферальная система
- **duels** - дуэли между игроками
- **user_stats** - статистика пользователей
- **broadcasts** - рассылки
- **settings** - настройки системы
- **admins** - администраторы
- **channel_posts** - посты в канале

## 🎲 API Endpoints

### Игра в кубик:

- `POST /api/games/dice/higher-lower` - Больше/Меньше
- `POST /api/games/dice/even-odd` - Четное/Нечетное
- `POST /api/games/dice/exact-number` - Грань (угадать число)
- `POST /api/games/dice/sector` - Сектор
- `POST /api/games/dice/duel` - Дуэль с казино
- `POST /api/games/dice/double` - 2X2
- `POST /api/games/dice/triple` - 3X3
- `POST /api/games/dice/sequence` - Подряд (3 числа)

### Пользователи:

- `POST /api/user` - Создать/обновить пользователя
- `GET /api/user/:userId/balance` - Получить баланс
- `GET /api/user/:userId/history` - История игр

### Игры:

- `GET /api/games` - Список игр
- `GET /api/games/:gameId/modes` - Режимы игры

## 📝 TODO (что осталось реализовать)

- [ ] Криптобот для пополнения/вывода
- [ ] Модерация выводов от $10
- [ ] Остальные игры (Боулинг, Футбол, Баскетбол, Дартс, Слоты, КНБ)
- [ ] Дуэли между игроками
- [ ] Админ-панель
- [ ] Рассылки с медиа
- [ ] Профиль игрока со статистикой
- [ ] Канал со ставками

## 🔧 Отладка

### Проверка подключения к БД:

```bash
psql -h localhost -U postgres -d casino_bot
```

### Просмотр таблиц:

```sql
\dt
```

### Просмотр данных:

```sql
SELECT * FROM users;
SELECT * FROM games;
SELECT * FROM game_modes;
```

### Логи:

Все логи выводятся в консоль при запуске через `npm run dev`.

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте подключение к PostgreSQL
2. Убедитесь что все переменные в `.env` заполнены
3. Проверьте логи в консоли
4. Убедитесь что TELEGRAM_BOT_TOKEN корректный

---

**Версия:** 1.0
**Дата:** Январь 2026
