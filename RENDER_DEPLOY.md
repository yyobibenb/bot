# 🚀 Деплой на Render.com

## Быстрая инструкция

### 1. Создайте PostgreSQL базу на Render

1. Зайдите на https://render.com
2. Нажмите **New +** → **PostgreSQL**
3. Заполните:
   - **Name:** casino-bot-db
   - **Database:** casino_bot
   - **User:** оставьте по умолчанию
   - **Region:** выберите ближайший
   - **Plan:** Free (для теста)
4. Нажмите **Create Database**
5. **Сохраните** Internal Database URL (понадобится позже)

### 2. Создайте Web Service

1. Нажмите **New +** → **Web Service**
2. Подключите ваш GitHub репозиторий
3. Заполните:
   - **Name:** casino-bot
   - **Region:** тот же что и БД
   - **Branch:** main (или ваша ветка)
   - **Runtime:** Node
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`
   - **Plan:** Free

### 3. Настройте Environment Variables

В разделе **Environment** добавьте переменные:

```
# Обязательные:
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
DATABASE_URL=internal_database_url_из_шага_1

# Опциональные:
NODE_ENV=production
PORT=5000
WEB_APP_URL=https://ваш-сервис.onrender.com
```

**Важно:** `DATABASE_URL` автоматически создаётся Render, но убедитесь что он правильный.

### 4. Deploy

1. Нажмите **Create Web Service**
2. Render автоматически развернёт приложение
3. Дождитесь окончания билда (3-5 минут)

### 5. Настройте Telegram бота

После деплоя:

1. Скопируйте URL вашего сервиса (например: `https://casino-bot.onrender.com`)
2. Обновите `WEB_APP_URL` в Environment Variables
3. Настройте Mini App через @BotFather:
   ```
   /myapps
   ```
   - Выберите бота
   - Укажите Web App URL: `https://casino-bot.onrender.com`

### 6. Проверьте работу

1. Откройте вашего бота в Telegram
2. Нажмите `/start`
3. Нажмите кнопку "🚀 Открыть Mini App"
4. Должен открыться интерфейс казино

## 🔧 Важные моменты

### Free план Render

- **Спит через 15 минут** неактивности
- Первый запрос после сна занимает 30-50 секунд
- **50 часов** бесплатно в месяц

### Keep-alive

Приложение автоматически пингует себя каждые 4 минуты, но на free плане это не поможет избежать сна.

Для production используйте платный план ($7/мес).

### SSL/HTTPS

Render автоматически предоставляет HTTPS - ничего настраивать не нужно!

### База данных

Free PostgreSQL на Render:
- **1GB** хранилища
- Автоматические бэкапы
- После 90 дней неактивности будет удалена

## 📊 Мониторинг

В Render Dashboard вы можете:
- Смотреть логи в реальном времени
- Проверять использование ресурсов
- Перезапускать сервис
- Управлять переменными окружения

## 🐛 Отладка

### Смотрим логи:

1. Зайдите в Dashboard → Ваш сервис
2. Вкладка **Logs**
3. Ищите ошибки

### Типичные проблемы:

**"Cannot connect to database"**
- Проверьте что `DATABASE_URL` правильный
- Убедитесь что БД и сервис в одном регионе

**"Telegram bot не отвечает"**
- Проверьте `TELEGRAM_BOT_TOKEN`
- Убедитесь что `WEB_APP_URL` указывает на ваш Render URL

**"Mini App не открывается"**
- Проверьте что URL в BotFather совпадает с Render URL
- URL должен быть HTTPS

## 🔄 Обновление

После изменения кода:

```bash
git add .
git commit -m "Update"
git push
```

Render автоматически задеплоит новую версию!

## 💰 Upgrade

Для production рекомендуется:

**Web Service:** Starter ($7/мес)
- Без сна
- Больше CPU/RAM
- Приоритетная поддержка

**PostgreSQL:** Starter ($7/мес)
- 10GB хранилища
- Ежедневные бэкапы
- Гарантированное время работы

---

**Готово!** Ваш Casino Bot теперь в production! 🎰
