# 🚀 Быстрый деплой на Render

## Шаг 1: Создайте бота
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Придумайте имя и username для бота
4. Сохраните полученный **токен** (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Деплой на Render

### Вариант А: Через Blueprint (автоматически)
1. Зайдите на [render.com](https://render.com) и залогиньтесь через GitHub
2. Нажмите **"New"** → **"Blueprint"**
3. Выберите этот репозиторий
4. Render автоматически создаст:
   - Web Service (бот)
   - PostgreSQL базу данных
5. Добавьте переменные окружения (см. ниже)
6. Нажмите **"Apply"** и ждите деплоя!

### Вариант Б: Вручную
1. Зайдите на [render.com](https://render.com)
2. Нажмите **"New"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name:** `garant-bot`
   - **Region:** Frankfurt (ближе к России)
   - **Branch:** выберите вашу ветку
   - **Runtime:** Node
   - **Build Command:** `npm ci && npm run build`
   - **Start Command:** `npm start`
5. Добавьте переменные окружения (см. ниже)
6. Нажмите **"Create Web Service"**

## Шаг 3: Настройте базу данных
1. В Render нажмите **"New"** → **"PostgreSQL"**
2. **Name:** `garant-bot-db`
3. **Region:** Frankfurt (та же что и бот)
4. **Plan:** Free
5. Нажмите **"Create Database"**
6. Скопируйте **Internal Database URL**
7. Добавьте его в переменные окружения бота как `DATABASE_URL`

## Шаг 4: Переменные окружения

Добавьте в настройках Web Service:

```bash
TELEGRAM_BOT_TOKEN=<ваш токен от BotFather>
DATABASE_URL=<Internal Database URL из шага 3>
SESSION_SECRET=<любая случайная строка, например: mySecretKey123456>
WEB_APP_URL=<ваш URL на Render, например: https://garant-bot.onrender.com>
PORT=10000
```

### Где взять:
- `TELEGRAM_BOT_TOKEN` - от [@BotFather](https://t.me/BotFather)
- `DATABASE_URL` - из настроек вашей PostgreSQL базы на Render (вкладка "Info")
- `SESSION_SECRET` - придумайте любую сложную строку
- `WEB_APP_URL` - появится после первого деплоя (будет выглядеть как `https://your-app-name.onrender.com`)

## Шаг 5: Настройте Mini App в BotFather
1. Откройте [@BotFather](https://t.me/BotFather)
2. Выберите ваш бот
3. Отправьте `/mybots`
4. Выберите бот → **Bot Settings** → **Menu Button**
5. Укажите URL: `https://your-app-name.onrender.com`
6. Готово!

## Шаг 6: Проверка
1. Найдите ваш бот в Telegram
2. Отправьте `/start`
3. Должна появиться кнопка **"🚀 Открыть Mini App"**
4. Нажмите - должен открыться веб-интерфейс

## ⚠️ Важно

1. **Free tier Render:**
   - Бот засыпает после 15 минут неактивности
   - Первый запрос после сна будет долгим (30-60 сек)
   - Для production используйте платный план

2. **База данных:**
   - Free PostgreSQL на Render удаляется через 90 дней
   - Делайте бэкапы важных данных
   - Или используйте внешнюю БД (например, ElephantSQL)

3. **Логи:**
   - Смотрите логи в Render Dashboard → Your Service → Logs
   - Там видно все ошибки и события

## 🔧 Обновление

При каждом push в вашу ветку Render автоматически:
1. Подтягивает изменения
2. Собирает проект
3. Перезапускает бот

**Автодеплой включен!** Просто пушьте код и все обновится само.

## 📞 Проблемы?

1. **Бот не отвечает:** Проверьте логи в Render
2. **База данных не работает:** Проверьте `DATABASE_URL` в переменных окружения
3. **Mini App не открывается:** Проверьте что `WEB_APP_URL` настроен правильно
4. **403 ошибка:** Проверьте токен бота

## 🎉 Готово!

Ваш бот работает 24/7 на Render!
