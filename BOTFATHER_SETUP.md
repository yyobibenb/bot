# 🤖 Настройка BotFather для Mini App

## ⚠️ КРИТИЧЕСКИ ВАЖНО!

Чтобы Mini App получал `telegram_id` пользователя, **ОБЯЗАТЕЛЬНО** нужно настроить URL Mini App в BotFather!

Без этого Telegram **НЕ ПЕРЕДАСТ** данные пользователя в `initDataUnsafe`.

---

## 📋 Пошаговая инструкция

### 1. Открой BotFather

Найди в Telegram: [@BotFather](https://t.me/BotFather)

### 2. Настрой Mini App URL

Отправь команду:
```
/mybots
```

Выбери своего бота из списка

Нажми **"Bot Settings"** → **"Menu Button"** → **"Configure menu button"**

### 3. Введи URL

Когда BotFather спросит "Send me the URL", отправь:
```
https://bot-rl59.onrender.com
```

**ВАЖНО:**
- URL должен быть **БЕЗ параметров** (без `?tg_id=123`)
- URL должен начинаться с `https://`
- URL должен быть доступен публично

### 4. Введи название кнопки

Когда BotFather спросит "Send me the button text", отправь:
```
🚀 Открыть Casino
```

---

## ✅ Проверка

После настройки:

1. Отправь `/start` своему боту
2. Нажми кнопку **"🚀 Открыть Mini App"**
3. Открой **DevTools** (F12) → **Console**
4. Смотри логи:

**Если всё ОК:**
```
✅ Telegram WebApp ready
✅ НАЙДЕН ID из Telegram SDK: 123456789
```

**Если НЕ ОК:**
```
❌ Telegram SDK не содержит данных пользователя
```
→ Значит не настроен URL в BotFather!

---

## 🔍 Как это работает

### Когда Mini App открывается через web_app кнопку:

1. **BotFather ЗНАЕТ** какой URL открыть (ты ему сказал)
2. Telegram **автоматически добавляет** данные пользователя в `initDataUnsafe`
3. Твой frontend читает `window.Telegram.WebApp.initDataUnsafe.user.id`

### Когда Mini App открывается напрямую (через браузер):

1. Telegram **НЕ ЗНАЕТ** что это Mini App
2. `initDataUnsafe` **пустой**
3. Нужен fallback на URL параметры (но это НЕ штатный режим)

---

## 🚨 Частые проблемы

### "ID не загружается"
→ Не настроен URL в BotFather

### "initDataUnsafe пустой"
→ Не настроен URL в BotFather ИЛИ открыл Mini App напрямую в браузере

### "URL параметры не передаются"
→ Это нормально! Telegram удаляет параметры из web_app кнопок
→ Используй настройку через BotFather вместо параметров

---

## 📚 Дополнительно

### Изменить URL позже

```
/mybots → Выбрать бота → Bot Settings → Menu Button → Configure menu button
```

### Отключить Mini App

```
/mybots → Выбрать бота → Bot Settings → Menu Button → Disable menu button
```

---

## ✨ Итого

Для работы Mini App нужно **ДВА** URL:

1. **В .env файле:** `WEB_APP_URL=https://bot-rl59.onrender.com`
   - Используется ботом для keyboard кнопок

2. **В BotFather:** Menu Button URL = `https://bot-rl59.onrender.com`
   - Используется Telegram для передачи данных

Оба должны быть **одинаковые**!
