# P2P Гарант-Бот

Telegram бот для безопасных P2P сделок с USDT TRC20.

## 🚀 Возможности

- 🔐 Безопасные сделки с автоматическим эскроу
- 💼 Автоматическое создание кошельков для каждой сделки
- 📱 Удобный Mini App интерфейс
- 🔒 PIN-защита приватных ключей
- ⚖️ Система арбитража для разрешения споров
- ⭐ Система репутации и отзывов

## 📋 Требования

- Node.js 18+
- PostgreSQL 12+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## ⚙️ Установка

1. **Клонируйте репозиторий:**
```bash
git clone <your-repo-url>
cd bot
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Настройте переменные окружения:**
```bash
cp .env.example .env
```

Отредактируйте `.env` файл и заполните:
- `TELEGRAM_BOT_TOKEN` - токен от BotFather
- `DATABASE_URL` или настройки PostgreSQL
- `WEB_APP_URL` - URL вашего сервера
- `SESSION_SECRET` - секретный ключ для сессий

4. **Настройте базу данных:**
```bash
# База данных инициализируется автоматически при первом запуске
```

## 🏃 Запуск

### Разработка
```bash
npm run dev
```

### Production
```bash
npm run build
npm start
```

### Docker
```bash
docker build -t garant-bot .
docker run -p 5000:5000 --env-file .env garant-bot
```

## 🔧 Настройка Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather)
2. Выберите ваш бот
3. Перейдите в `Bot Settings` -> `Menu Button`
4. Укажите URL вашего Mini App: `https://your-domain.com`

## 📝 Структура проекта

```
bot/
├── src/
│   ├── bot/          # Telegram бот
│   ├── server/       # Express сервер для Mini App
│   ├── services/     # Бизнес логика
│   ├── db/           # База данных
│   └── utils/        # Утилиты
├── .env.example      # Пример конфигурации
├── Dockerfile        # Docker конфигурация
└── package.json      # Зависимости
```

## 🌐 Deployment

### Render.com
1. Создайте новый Web Service
2. Подключите репозиторий
3. Добавьте переменные окружения
4. Deploy!

### Railway.app
1. Создайте новый проект
2. Добавьте PostgreSQL
3. Подключите репозиторий
4. Настройте переменные окружения

### Fly.io
```bash
fly launch
fly deploy
```

## 🔐 Безопасность

- Все приватные ключи шифруются в базе данных
- PIN-коды хешируются с использованием bcrypt
- Используется AES-256 для шифрования
- Seed-фразы генерируются локально

## 📖 API Endpoints

- `GET /` - Mini App интерфейс
- `GET /health` - Health check endpoint

## 🤝 Как работают сделки

1. **Создание сделки:** Продавец/покупатель создает сделку
2. **Генерация кошелька:** Автоматически создается эскроу-кошелек
3. **Оплата:** Покупатель переводит USDT на кошелек сделки
4. **Подтверждение:** Покупатель подтверждает получение товара/услуги
5. **Завершение:** Продавец получает seed-фразу для доступа к средствам

## ⚖️ Арбитраж

При возникновении споров:
1. Любая сторона может запросить арбитраж
2. Арбитр рассматривает обе стороны
3. Принимается решение кому передать средства
4. Seed-фраза отправляется победителю

## 📱 Команды бота

- `/start` - Начать работу с ботом
- `/cancel` - Отменить текущую операцию

## 🛠 Технологии

- **Backend:** Node.js, TypeScript, Express
- **Database:** PostgreSQL
- **Blockchain:** TronWeb (USDT TRC20)
- **Bot:** node-telegram-bot-api
- **Security:** crypto, AES-256, bcrypt

## 📄 Лицензия

ISC

## 👨‍💻 Поддержка

Для вопросов и поддержки создайте Issue в репозитории.
