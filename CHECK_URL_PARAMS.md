# 🔍 Проверка передачи данных через URL

## Проблема: "Не закидываются данные"

Если в профиле Mini App показывает:
```
Источник: 📱 Telegram SDK
URL параметры: ❌ Нет
```

Это значит данные НЕ передаются через URL параметры из бота.

---

## 🐛 Причины почему данные не передаются

### 1. ❌ Не установлен WEB_APP_URL в .env

**Проблема:** В файле `.env` переменная `WEB_APP_URL` установлена неправильно или отсутствует

**Решение:**

1. Откройте файл `.env` в корне проекта
2. Найдите строку:
   ```
   WEB_APP_URL=https://your-domain.com
   ```
3. Замените на РЕАЛЬНЫЙ URL вашего приложения:
   ```
   WEB_APP_URL=https://your-actual-app.onrender.com
   ```
   или
   ```
   WEB_APP_URL=https://yourdomain.com
   ```

4. **ПЕРЕЗАПУСТИТЕ БОТ** (иначе изменения не применятся!)

---

### 2. ❌ Бот не перезапущен после изменений

**Проблема:** Вы изменили код бота (`telegramBot.ts`) или `.env`, но не перезапустили приложение

**Решение:**

```bash
# Если запущено через npm
npm run dev

# Или если на продакшене
npm start

# Или если через PM2
pm2 restart bot
```

---

### 3. ❌ Используется старая кнопка без URL параметров

**Проблема:** Пользователь использует кнопку из старого сообщения бота (когда еще не была добавлена передача параметров)

**Решение:**

1. Отправьте `/start` ЗАНОВО в боте
2. Получите НОВУЮ кнопку "🚀 Открыть Mini App"
3. Нажмите на эту новую кнопку

---

## ✅ Как проверить что данные ПЕРЕДАЮТСЯ

### Способ 1: Тестовая страница

1. Откройте тестовую страницу **напрямую из бота**:
   - Отправьте `/start` боту
   - Нажмите "🚀 Открыть Mini App"
   - В адресной строке замените `/` на `/test-params.html`

2. Вы увидите страницу с информацией:
   ```
   Telegram ID: 123456789 ✅
   Имя: John ✅
   Фамилия: Doe ✅
   Username: johndoe ✅
   Фото: ✅ Есть

   Данные переданы через URL: ✅ ДА
   ```

### Способ 2: Консоль сервера бота

Когда пользователь нажимает `/start`, в консоли сервера должно появиться:

```
📋 Формирую URL с данными пользователя:
  - telegram_id: 123456789
  - first_name: John
  - last_name: Doe
  - username: johndoe
  - is_premium: false
  - photo_url: https://api.telegram.org/file/bot.../photo.jpg
✅ Сформирован URL: https://your-app.com?user_id=123456789&first_name=John&last_name=Doe&username=johndoe&photo_url=https%3A%2F%2F...&is_premium=false
```

**Если этого НЕТ** → бот не был перезапущен после изменений!

### Способ 3: Профиль в Mini App

В профиле должна появиться карточка:

```
📋 Данные пользователя
Telegram ID: 123456789
Источник: ✅ URL параметры  ← ДОЛЖНО БЫТЬ ТАК!
URL параметры: ✅ Да
Фото: ✅ Есть
```

Если показывает `📱 Telegram SDK` → данные НЕ передаются через URL!

### Способ 4: Консоль браузера (F12)

Откройте Mini App и нажмите F12 (DevTools). В консоли должно быть:

```
🔍 URL параметры: {
  user_id: "123456789",
  first_name: "John",
  last_name: "Doe",
  username: "johndoe",
  photo_url: "https://...",
  is_premium: "false"
}
✅ Данные пользователя из URL параметров: {...}
```

**Если показывает:**
```
🔍 URL параметры: {
  user_id: null,
  first_name: null,
  ...
}
```

→ Данные НЕ передаются через URL!

---

## 📝 Пошаговая инструкция исправления

### ШАГ 1: Проверьте .env

```bash
cat .env | grep WEB_APP_URL
```

Должно показать ваш РЕАЛЬНЫЙ URL:
```
WEB_APP_URL=https://your-app.onrender.com
```

**Если показывает:**
- `https://your-domain.com` → ❌ Неправильно, это дефолтное значение
- Пусто или нет строки → ❌ Нужно добавить

**Исправьте:**
```bash
nano .env
# Найдите строку WEB_APP_URL и замените на ваш реальный URL
# Сохраните: Ctrl+O, Enter, Ctrl+X
```

### ШАГ 2: Перезапустите бота

```bash
# Остановите текущий процесс (Ctrl+C если запущен в терминале)
# Затем запустите заново:
npm run dev
```

### ШАГ 3: Отправьте /start в боте ЗАНОВО

Важно получить НОВУЮ кнопку после перезапуска!

### ШАГ 4: Проверьте консоль сервера

После `/start` должно появиться:
```
📋 Формирую URL с данными пользователя:
  - telegram_id: ...
  ...
✅ Сформирован URL: https://...?user_id=...
```

### ШАГ 5: Откройте Mini App и проверьте

В профиле должно показать:
```
Источник: ✅ URL параметры
```

---

## 🚨 Частые ошибки

### Ошибка 1: "Все равно не работает"
**Причина:** Бот не был перезапущен
**Решение:** Убедитесь что процесс бота был перезапущен!

### Ошибка 2: "В консоли нет логов"
**Причина:** Смотрите не ту консоль (браузер вместо сервера)
**Решение:** Логи "📋 Формирую URL..." показываются в **консоли СЕРВЕРА** (где запущен бот), а не в браузере!

### Ошибка 3: "URL параметры есть но не те"
**Причина:** Кэш браузера или старая сессия
**Решение:**
1. Закройте Mini App полностью
2. Отправьте `/start` заново
3. Откройте по новой кнопке

### Ошибка 4: "photo_url нет"
**Причина:** У пользователя нет фото профиля в Telegram или Bot API не может его загрузить
**Решение:** Это нормально, просто не будет показываться аватар

---

## 🎯 Контрольный список

- [ ] `.env` файл содержит правильный `WEB_APP_URL`
- [ ] Бот был перезапущен после изменений
- [ ] Отправил `/start` заново в боте
- [ ] Получил новую кнопку "🚀 Открыть Mini App"
- [ ] В консоли сервера появились логи "📋 Формирую URL..."
- [ ] Открыл Mini App по новой кнопке
- [ ] В профиле показывает "Источник: ✅ URL параметры"

Если все галочки стоят, но данные все равно не передаются - проверьте:
1. Нет ли ошибок в консоли сервера
2. Правильно ли работает buildWebAppUrlWithParams()
3. Не блокируются ли параметры на уровне Telegram (редко, но бывает)

---

## 📞 Дополнительная отладка

### Проверить что возвращает getWebAppUrl():

Добавьте временно в `telegramBot.ts` строку 36:
```typescript
const baseUrl = this.getWebAppUrl();
console.log('🌐 Base URL:', baseUrl);  // ← добавьте эту строку
```

Перезапустите бота и отправьте `/start`.
Должно показать:
```
🌐 Base URL: https://your-app.onrender.com
```

Если показывает `https://your-domain.com` или `undefined` → проблема в `.env`!
